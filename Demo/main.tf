provider "aws" {
  region     = "us-west-2"
  access_key = "my-access-key" #keep the access key using aws config command on aws CLI
  secret_key = "my-secret-key" #keep the secret key using aws config command on aws CLI
}